---
phase: 03-readme-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [README.md]
autonomous: true
requirements: [README-01, README-02, README-03]

must_haves:
  truths:
    - "README has a Security Setup section after Quick Start"
    - "Security Setup links to the wiki Security page"
    - "Detailed Cloudflare Access setup is NOT in README"
    - "Quick Start remains self-sufficient for basic deployment"
    - "README is noticeably shorter than 510 lines"
  artifacts:
    - path: "README.md"
      provides: "Slimmed README with wiki links"
      contains: "## Security Setup"
  key_links:
    - from: "README.md"
      to: "wiki/Security"
      via: "GitHub wiki URL"
      pattern: "wiki/Security"
---

<objective>
Slim README.md from 510 lines to ~250 lines by moving detailed security/configuration content to wiki pages, adding a prominent Security Setup section with wiki links.

Purpose: Make README scannable while directing users to comprehensive wiki documentation for advanced setup.
Output: Slimmed README.md with prominent wiki links using GitHub callout boxes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-readme-integration/03-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Security Setup section after Quick Start</name>
  <files>README.md</files>
  <read_first>
    - README.md (current state, identify exact line number where Quick Start ends)
    - .planning/phases/03-readme-integration/03-CONTEXT.md (D-01, D-02, D-06 decisions)
  </read_first>
  <action>
Insert a new "## Security Setup" section immediately after the Quick Start section (after line 98, before the current "## Configuration" section).

The section must contain:

1. A brief intro paragraph (2-3 sentences max) stating that Setup Manager HUD supports authentication and webhook token validation.

2. A GitHub callout box linking to the wiki Security page:
```markdown
> [!TIP]
> **Full security setup guide:** [Security](https://github.com/motionbug/setupmanagerhud/wiki/Security) covers webhook token configuration, Cloudflare Access setup, and rate limiting.
```

3. A quick summary list (3 bullet points max):
   - Cloudflare Access protects the dashboard (link to wiki)
   - Webhook tokens validate device requests (link to wiki) 
   - Rate limiting prevents abuse (link to wiki)

Total section length: ~15-20 lines.

Per D-01: This section goes AFTER Quick Start.
Per D-02: Summary only, not the full 200-line setup.
Per D-06: Use GitHub callout boxes (`> [!TIP]`).
  </action>
  <acceptance_criteria>
    - README.md contains `## Security Setup` header
    - Security Setup section appears after `## Quick Start` and before `## Configuration`
    - README.md contains `> [!TIP]` callout with wiki link
    - README.md contains `https://github.com/motionbug/setupmanagerhud/wiki/Security`
    - Security Setup section is between 10-25 lines (not the 200-line full setup)
  </acceptance_criteria>
  <verify>
    <automated>grep -n "## Security Setup" README.md && grep "wiki/Security" README.md && awk '/## Security Setup/,/^## / {count++} END {print "Section lines:", count}' README.md</automated>
  </verify>
  <done>Security Setup section exists after Quick Start with wiki link in callout box</done>
</task>

<task type="auto">
  <name>Task 2: Remove Securing the Dashboard section, add wiki links where appropriate</name>
  <files>README.md</files>
  <read_first>
    - README.md (identify exact line range of "## Securing the Dashboard" section: ~lines 202-403)
    - .planning/phases/03-readme-integration/03-CONTEXT.md (D-03, D-04, D-05 decisions)
  </read_first>
  <action>
Remove the entire "## Securing the Dashboard" section (approximately lines 202-403, roughly 200 lines). This content now lives in the wiki Security page created in Phase 2.

Also remove:
- "### Optional: Rate Limiting the Webhook Endpoint" section (per D-05, moved to wiki)
- Any redundant security explanations in other sections

Update references in remaining sections:
1. In "### Connecting Setup Manager" section, after the plist example, add a callout:
```markdown
> [!NOTE]
> To require authentication on webhook requests, see [Webhook Token Setup](https://github.com/motionbug/setupmanagerhud/wiki/Security#webhook-token-setup-required-for-production) in the wiki.
```

2. In "### Test with a Sample Webhook" section, keep the basic curl example but remove any detailed token testing content.

3. Remove any orphaned references to "see below" or "see above" that pointed to the deleted sections.

Per D-03: "Securing the Dashboard" section (~200 lines) moves entirely to wiki.
Per D-04: Configuration details beyond KV setup move to wiki.
Per D-05: Rate limiting details move to wiki.
  </action>
  <acceptance_criteria>
    - README.md does NOT contain `## Securing the Dashboard` header
    - README.md does NOT contain `### Optional: Rate Limiting` section
    - README.md does NOT contain `### Step-by-Step Setup` (was under Securing the Dashboard)
    - README.md does NOT contain `### Access Configuration Summary` table
    - README.md contains `wiki/Security#webhook-token-setup` link in Connecting Setup Manager section
    - Line count reduced by at least 150 lines from original 510
  </acceptance_criteria>
  <verify>
    <automated>! grep -q "## Securing the Dashboard" README.md && ! grep -q "### Optional: Rate Limiting" README.md && grep -q "wiki/Security" README.md && wc -l < README.md</automated>
  </verify>
  <done>Securing the Dashboard section removed, wiki links added where content was referenced</done>
</task>

<task type="auto">
  <name>Task 3: Slim Configuration section, verify Quick Start self-sufficiency</name>
  <files>README.md</files>
  <read_first>
    - README.md (current Configuration section structure)
    - .planning/phases/03-readme-integration/03-CONTEXT.md (D-07, D-08, D-09 decisions)
  </read_first>
  <action>
Review and slim the "## Configuration" section:

1. KEEP: "### KV Namespace (Required)" section entirely - this is needed for Quick Start to work (per D-07, D-09)

2. KEEP: "### Connecting Setup Manager" section - basic plist example is essential

3. KEEP: "### Test with a Sample Webhook" section - useful for verification

4. ADD: A callout at the end of Configuration section linking to wiki for advanced configuration:
```markdown
> [!TIP]
> **Advanced configuration:** See the [Configuration](https://github.com/motionbug/setupmanagerhud/wiki/Configuration) wiki page for environment variables, wrangler.toml reference, and health check endpoints.
```

5. VERIFY Quick Start self-sufficiency:
   - Quick Start section must still have: deploy button, manual deploy steps, GitHub Actions steps
   - Quick Start section must still reference KV namespace setup
   - Quick Start section must NOT require reading wiki to complete basic deployment

Per D-07: Quick Start keeps deploy options and KV namespace setup.
Per D-08: Quick Start does NOT include Cloudflare Access or webhook token setup.
Per D-09: Basic deployment works without reading the wiki.
  </action>
  <acceptance_criteria>
    - README.md contains `### KV Namespace (Required)` section (still present)
    - README.md contains `### Connecting Setup Manager` section (still present)
    - README.md contains `wiki/Configuration` link
    - Quick Start section contains `npx wrangler kv namespace create WEBHOOKS`
    - Quick Start section does NOT contain `CF_ACCESS_AUD` (Cloudflare Access config)
    - Quick Start section does NOT contain `WEBHOOK_SECRET` setup steps
    - Total README.md line count is between 200-300 lines
  </acceptance_criteria>
  <verify>
    <automated>grep -q "### KV Namespace" README.md && grep -q "wiki/Configuration" README.md && ! grep -q "CF_ACCESS_AUD" README.md | head -50 && wc -l < README.md</automated>
  </verify>
  <done>Configuration section slimmed with wiki link, Quick Start remains self-sufficient</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | This is documentation-only work, no runtime code changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | README.md | accept | Documentation is public; no secrets in README |

No security-sensitive changes in this phase - purely documentation restructuring.
</threat_model>

<verification>
After all tasks complete:
1. README.md line count is between 200-300 (down from 510)
2. Contains "## Security Setup" section after Quick Start
3. Contains wiki links using `> [!TIP]` or `> [!NOTE]` callouts
4. Does NOT contain "## Securing the Dashboard" section
5. Quick Start section remains unchanged except for any orphaned references
6. `npm run typecheck` still passes (README changes don't affect types)
</verification>

<success_criteria>
1. README has prominent "Security Setup" section linking to wiki Security page (README-01)
2. Detailed configuration content moved to wiki, replaced with links (README-02)
3. Quick Start section remains self-sufficient for basic deployment (README-03)
4. README is noticeably shorter: target 200-300 lines (down from 510)
5. All user decisions D-01 through D-09 honored
</success_criteria>

<output>
After completion, create `.planning/phases/03-readme-integration/03-01-SUMMARY.md`
</output>
